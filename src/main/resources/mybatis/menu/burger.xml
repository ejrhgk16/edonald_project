<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edonald.menu.dao.BurgerMapper">
	<select id="listAll" resultType="com.edonald.menu.dto.BurgerDto">
		<![CDATA[ select b_seq, burger_name, burger_e_name,burger_btn_img_path, burger_kcal, burger_n_img_path, burger_detail_img_path, burger_detail_comment, burger_allergy 
         from( select @rownum:=@rownum+1 as rn, bno, title, 
         content, writer, regdate, updateDate from burger order by b_seq desc where rownum<= #{end} 
          )where rn>(#{start}) ]]>
	</select>
	
	<insert id="insert">
		INSERT INTO burger VALUES(#{burger_code},#{burger_name},#{burger_kcal},#{burger_img_path},#{burger_status},#{burger_n_img_path},#{category_code},#{burger_e_name},#{burger_detail_img_path},#{burger_price},#{burger_btn_img_path},#{burger_detail_comment},#{burger_allergy})
	</insert>
</mapper>